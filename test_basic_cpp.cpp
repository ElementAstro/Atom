#include <iostream>

int main() {
    std::cout << "=== BASIC C++ TEST ===" << std::endl;
    std::cout << "This is a basic C++ program without any Atom dependencies." << std::endl;
    std::cout << "If this works, the issue is with Atom libraries." << std::endl;
    std::cout << "If this fails, the issue is with the build environment." << std::endl;
    return 0;
}
